package Graphic.Map;

import Logic.Map.Map;
import Logic.Map.Play;

import java.awt.BorderLayout;

import javax.swing.JFrame;


public class Frame_GraphicPlay extends JFrame{
	private static final long serialVersionUID = 1L;
	
	protected InformationPanel information;
	protected Play play;
	
	public Frame_GraphicPlay(){
		information= new InformationPanel(p);
		
		play= new Play("Mapas/Mapa 1.txt",this);
		setTitle("Battle City");
		setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		setLayout(new BorderLayout());
		setSize(m.getGraphicMap().getWidth()+information.getWidth()+100,m.getGraphicMap().getHeight());
		setVisible(true);
		setLocationRelativeTo(null);
		setResizable(false);
		
		
		add(m.getGraphicMap(), BorderLayout.CENTER);
		add(information, BorderLayout.EAST);
	}
	public InformationPanel getInformationPanel(){
		return information;
	}
}